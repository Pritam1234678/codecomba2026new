
# Build files
target/
bin/
build/

# IDE files
.idea/
.vscode/
*.iml

# Node modules
node_modules/
frontend/node_modules/

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# ─── Secrets / credentials ──────────────────────────────────────────────────
# Catch-all patterns for any cert / key file that may land in the repo.
# These are intentionally aggressive — false positives are easier to fix
# than a single leaked credential.
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.cert
*.der
*.jks
*.keystore
*.truststore
*.asc
*.gpg
*.kdbx
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*-key.pem
*_key.pem
*-cert.pem
*_cert.pem
*.ssh
ssh-key-*
*authorized_keys*
*known_hosts*
codecombat-key.pem

# Sensitive configuration files
src/main/resources/application.properties
src/main/resources/application-*.properties
frontend/.env
frontend/.env.local
frontend/.env.production
.env
# .env.example is intentionally NOT ignored — it's the template for teammates

# Uploaded user files — not for git
uploads/
oci_vm_key
uploads/
stitch_code_combat_ultimate_arena_design/
DEPLOYMENT_CHANGES.md
ORACLE_VM_DEPLOY.md
PROBLEM_GUIDE.md
stitch_code_combat_ultimate_arena_design/
.env.production-vm
bench_admin.sh
bench_full.sh
test_duel.sh
bench_routes.sh
bench_proctoring.sh
deploy_proctoring_smoke.sh
frontend/images/
