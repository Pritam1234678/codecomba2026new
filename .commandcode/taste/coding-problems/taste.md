# coding-problems
- When creating coding problem test harnesses, ALL 5 languages (Java, C++, Python, JavaScript, C) must have real test cases — never hardcode "PASS" outputs or leave any language without working test cases. C harness must actually call the user's function and verify results, not just print PASS. Confidence: 0.95
- For C++ harnesses (and any language that supports classes), use `class` instead of `struct` for ALL data structures (TreeNode, ListNode, Node, etc.) — never a bare/"faux" `struct`; wherever a language has classes, always use `class`. The user emphatically stated this as a general rule: "cpp me faltu ka struct mat banana class banana jaha jis language me class hoga udhar hamesha class banana". Confidence: 0.88
- In starter code (inside USER_CODE_START), the definition of any custom class/struct (e.g., TreeNode/Node with constructor) appears ONLY as a LeetCode-style `/** Definition for singly-linked list. */` Javadoc comment block that documents the fields/constructor — never as an active definition in the user section. The REAL class/struct definition goes in the driver code OUTSIDE the USER_CODE_START markers so the harness compiles. The user's editable region shows only this documentation comment plus the `CodeCoder` class + method stub (nothing else). Applies to all 5 languages (Java, C++, C, Python, JS). Confidence: 0.93
- When a problem supplies a predefined class/struct whose implementation is hidden from the user (e.g., Node/ListNode, Shape, TreeNode provided by the harness), show that class's full implementation/signature clearly to the user in the starter code AND/OR problem statement — like LeetCode/GFG render class definitions — so the user can see the provided types and their fields/constructors instead of burying or obscuring them. Stated as a general rule ("whenever you create ANY predefined class"). Confidence: 0.85
- When inserting coding problems into the database, create one separate Python file per problem rather than a single batch script for all problems. Confidence: 0.80
- Use class name "CodeCoder" instead of "Solution" in all test harnesses (Java, C++, Python). Confidence: 0.85
- Include exactly 10 test cases in each harness (5 visible + 5 hidden). Confidence: 0.85
- Do NOT include solution hints or tips in the harness comments — only the problem requirement. Confidence: 0.75
- When inserting a coding problem into the DB, also update remaining.md — decrement the remaining count and remove the problem from the table. Confidence: 0.72
- Deploy coding problem scripts to VM using: python3 compile check → scp → ssh run + cleanup (python3 ... && rm ...; echo '===done'). Confidence: 0.70
- Add problems one at a time, in sequence (increment S.No and follow the order in remaining.md) — do NOT skip problems or batch-add multiple, even if they appear later in the table. Update remaining.md ✓, create and deploy the next Python script, then continue to the following problem. The user has granted full permission to proceed without asking — "dont ask for permission full permission" — so do not pause to ask "next?" after each problem; just keep going autonomously. The user advances the flow with terse commands like "next" when prompted, and expects the assistant to handle the whole add+deploy+tick cycle for each problem. Confidence: 0.93
- Write coding problem descriptions in English with proper line-by-line or bullet-point example explanations. Confidence: 0.70
- User refers to problems by their sheet number (e.g., "q28 add kar" = add problem #28) and explicitly requests any skipped problem (e.g., "next" led to q29, then user followed up with "q28 add kar"; also "39 add kar", "42 add kar" while work was on other numbers, and "add lower bound 66" with the number after the name) — they track the sheet and expect every problem in the list to get added eventually, and they may request problems out of sequence. Complexity is not a valid reason to skip: when the assistant suggested skipping #64 Beauty of All Substrings as "complex", the user immediately replied "64 add kar" and it was added normally. Confidence: 0.82
- After deploying a coding problem to the DB, update its status in Have_To_Add.md sheet from ❌ to ✅. The user actively tracks this and issues explicit follow-up commands to mark problems done — either one at a time (e.g., "44 ko mark kar") or batched in a single message (e.g., "44 45 46 ko tick kar"). If they request a tick for problems already marked ✅, verify the sheet first and just confirm rather than redoing work. Confidence: 0.92
- Harness output must follow the format `TC:<n>:PASS` (visible) / `TC:<n>:FAIL:hidden` (hidden); hidden tests never leak input/expected/got, and every test() call is wrapped in try-catch so one crash doesn't kill the remaining tests. For VISIBLE tests (TC1–5), the FAIL message must embed recognized debug keys the grader parses — `n=` (int param), `arr=` (array param), `exp=`/`expected=` (expected), `got=` (student output), `input=`, `target=`, `L=`/`R=` (range params) — so the student can debug; ONLY these prefixes may be used (the grader recognizes exactly these keys), never arbitrary ones, or FAIL parsing breaks. Confidence: 0.88
- Before inserting problems from a sheet, query the DB for existing problem titles and skip/avoid duplicates (e.g., match case-insensitively, handle title differences like "GCD of Two Numbers (Euclidean Algorithm)" vs "GCD of Two Numbers"). This includes catching differently-titled duplicates via keyword search — e.g., "Rotate Matrix 90°" from the sheet was skipped because "Rotate Image" (same problem) already existed in the DB; when a sheet problem is found to already exist, skip it and move to the next sheet problem, and mark it ✅ in Have_To_Add.md (e.g., #39 Rotate Matrix marked done because Rotate Image pid=116 exists; #42 Armstrong marked done because pid=77 exists). The user also proactively commands this: "45 ko tick kar de wo db me already ha" — explicitly instructing to just tick #45 GCD as done since it already exists in the DB, with no insert needed. Confidence: 0.90
- After inserting problems, restart the backend (`sudo systemctl restart codecombat`) to clear cache and verify the insert via psql (SELECT count / list problems and code_snippets). Confidence: 0.70
- When a harness-standard violation is discovered (e.g., C++ `struct` instead of `class`, C harness not calling the user function), go back and fix already-deployed problems in the DB — not just apply the fix to future problems (proactively scans existing scripts like q43_divisors.py for leftover stub harnesses and fixes them even when the user didn't ask). Confidence: 0.85
- To fix a harness issue in an already-deployed problem, create a small standalone fix script (e.g., fix_c_208.py) that connects to the DB, defines only the corrected language snippet, and runs an UPDATE on code_snippets for that specific problem_id + language; deploy it with the same scp → ssh run → cleanup flow. Confidence: 0.60
- When the user explicitly asks to add a problem ("X add kar") that was previously skipped or marked ✅ because a near-duplicate existed in the DB, honor the explicit request and insert it as its own separate entry with proper harnesses (e.g., "39 add kar" → Rotate Matrix 90° added as pid=209 despite Rotate Image pid=116 existing; "75 add kar" after the assistant had just marked #75 ✅ citing pid=100) — explicit add requests override the skip-duplicate heuristic. Confidence: 0.72
- When a user requests adding a problem ("X add kar") that is already in the DB under a near-identical title, first verify against the DB and inform them it exists — but if the user insists it was never properly added, re-insert it fresh as requested rather than refusing (pid=77 "Armstrong Number" turned out to have no local script, and the user's insistence led to a fresh insert as q42_armstrong.py). Confidence: 0.65
- A coding problem only counts as "added" when BOTH the DB entry exists AND a corresponding Python script is saved locally in scripts/ (e.g., q42_armstrong.py) — the user tracks the scripts directory and calls out when a script is missing ("nehi ha db me ... idhar bhi add nehi kia ha"), so verify both the DB and scripts/ before claiming a problem already exists. Confidence: 0.72
- For problems with multiple valid answers, the harness judges by a property any valid answer satisfies rather than an exact match. Longest Palindromic Substring compares result length; Sort Array By Parity II (q114) checks the structural parity condition `(i%2==0)==(v%2==0)` at every index instead of one fixed arrangement; the 2D-peak problem (q80/q85) decodes a flat `r*n+c` int and verifies the position is a genuine peak. Test comments must explain the property-based acceptance. Confidence: 0.80
- Name per-problem DB scripts as `q<sheet_number>_<shortname>.py` (all lowercase, no spaces) — e.g., q57_atoi.py, q59_rotatestring.py, q61_longestpal.py. Confidence: 0.65
- For problems whose answer is a 2D grid position (row, col), encode it as a single flat integer `r*n + c` and return that one int (C-friendly — avoids returning arrays or multiple values); the harness decodes `r = flat/n`, `c = flat%n` and validates the resulting position satisfies the required property (e.g., it is a genuine peak — strictly greater than all 4 neighbors), so ANY valid position passes. Repeated across q80 and q85. Confidence: 0.80
- Time limit scales with difficulty when inserting problems: Easy uses `time_limit=3.0` seconds, Medium uses `time_limit=5.0` seconds (observed Easy q82/q83 = 3.0, Medium q84 = 5.0). Confidence: 0.60
- When checking the DB for duplicates, distinguish "same problem under a different title" (skip it and tick ✅) from a "genuinely different problem with a similar name" — e.g., #46 "Prime Factorisation of a Number" links to GfG "Largest Prime Factor", which is NOT the same as the DB's existing "Prime Factors of a Number" (pid=130), so it was inserted fresh as its own entry (pid=211) with full harnesses rather than skipped. Confidence: 0.65
- When an auto-compact or session summary is stale and conflicts with direct evidence (e.g., it claims a problem is still in-flight/incomplete, but the DB already contains it with 5 snippets and the sheet already shows ✅), treat the summary as unreliable and establish ground truth BEFORE inserting: query the DB directly for existing titles/pids + snippet counts and read the fresh sheet (uncached grep), then continue from the true next problem. This avoids duplicate DB rows and wasted rework from blindly following a stale summary. Explicitly invoked during the q87–q93 reconciliation where the summary wrongly said "resume at q87", but 87–92 were already done (q87 pre-existed as pid=62, 88–92 existed); the assistant verified and correctly continued from q93 instead of redoing prior work. Confidence: 0.82

- User streamlines coding-problem requests with terse directives like "next question bas db me add kar de" (= "just add the next question to the DB"), implying they want the problem-add workflow reduced to the essential DB insertion rather than the full add+deploy+tick+restart cycle. Treat this as a signal to keep the flow minimal unless they ask for the heavier steps. Confidence: 0.60

- Before writing/inserting a coding problem's full harness, validate the expected test outputs with a standalone local Python script using the exact intended algorithm — run this confirmation step BEFORE authoring the harness, so expected answers are verified correct before they land in the DB. First seen for binary-search-on-answer problems (q90 speeds, q93 days, q94 capacities); strongly reinforced in the Sorting batch where each problem's expected outputs were validated immediately before its harness: q112 linear-search indices, q115 bucket-sort maximum gaps, q116 frequency-sort arrangements. Confidence: 0.85
- When the intended algorithm's output conflicts with a hand-computed expected value, run an independent brute-force reference (e.g., sorted(a+b)[k-1] for k-th element of two sorted arrays) to establish the correct answer and fix the hand error — do NOT trust the manual calculation when it disagrees with code. Observed in q98 where two hand values (112, 10) were wrong and brute force corrected them to 256 and 15. Confidence: 0.65

- User wants a single self-contained reference document (`document.md`) in the `Sheets/` folder that fully documents the problem-insertion workflow — DB connection/insert pattern, all 5 language harness rules (CodeCoder class, `TC:<n>:PASS` format, try-catch, 10 test cases), deployment via compile-check → sc
cp → ssh run + cleanup, and Have_To_Add.md ✅ updates — written so that any AI with zero prior context can add questions to the DB exactly like the assistant does, with no mistakes. Explicit request: "bana sehi se as new ai ko koi bhi context neho pata hoga" (build it properly since a new AI will have no context). The user repeated this request after the first attempt failed with a context-length error AND again after a connection error — treat it as a priority deliverable; the delivered doc should also capture practical gotchas like C-array size params, `int* returnSize` for returned arrays, `char* out` buffers, and the Python `\\\\n` escaping rule. Confidence: 0.95

- When ingesting a batch of problems, the user prefers an automated loop that adds them one-by-one up to ~10 at a time, tracking progress with a todo list, and continuing autonomously when the user issues a terse "next" command — spanning sheet boundaries and even mixing categories (e.g., the q112–q120 Sorting batch continued into q121–q122 OOPS within the same 10-problem todo). Within the loop each problem's expected outputs are validated with a quick local script immediately before its harness is written and deployed. Explicitly requested ("autometically loop me ... upto 10 question ad kar") and confirmed across the q100→q111 and q112→q122 batches. Confidence: 0.88

- Before declaring a problem finished, actually COMPILE AND RUN the full test harness end-to-end for all 5 languages using BOTH a correct solution (must produce `TC:n:PASS` for every case) and the bare stub (must produce `TC:n:FAIL`) — do NOT rely solely on a standalone local script that validates expected values, because the harness wiring itself (templates, try-catch, compiler invocations) has repeatedly come out broken. The user angrily complained "hamesha galat harness generate karta ha" (always generates the wrong harness) — validating expected values is not the same as proving the harness runs correctly. The VM has gcc/g++/javac/java/node/python3 available, so this end-to-end check is feasible. Confidence: 0.88

- Actually read and follow `Sheets/document.md` precisely when generating harnesses — the user pointed out the assistant was not reading it properly ("achhes e pad") and producing wrong harnesses as a result. Treat that doc as the authoritative source for harness structure (not just a thing to have created) rather than trusting memory of the rules. Confidence: 0.80

- The user sometimes does DB inserts themselves and hands the remaining problems off to the assistant (e.g., they manually added q112–q115 then said "116 add kar main kar dia 115 tak" = "I've done up to 115, you add 116"). Because the user directly writes to the DB, never assume the assistant is the sole author of problems — always query live DB state (titles/pids + snippet counts) before continuing the add workflow, so manual inserts aren't redone or skipped. Confidence: 0.65

- C harnesses using floating-point math (`fabs`, `sqrt`) must `#include <math.h>` (or `<cmath>` in C++) — both q124 (Interfaces) and q126 (Factory) failed to compile until the include was added; a repeatable rule for any future C/C++ float-math problem. Confidence: 0.85

- Problem-insertion scripts must be idempotent: before blindly `INSERT` a new problem row, look up an existing problem by case-insensitive title (`SELECT id FROM problems WHERE LOWER(title)=LOWER(%s)`); if found, reuse that pid, `DELETE` its old code_snippets, and `INSERT`/`UPDATE` the refreshed snippets+metadata instead of INSERTing a duplicate row. This is distinct from the "skip near-duplicate" rule (which avoids *different-titled* problems) — this handles *re-running the same script* to rewrite a harness (e.g., the q127–q129 LeetCode-style conversions) without spawning duplicate problems. Standardized across the q127–q130 scripts. Confidence: 0.78

- For VISIBLE (TC1–5) WRONG-ANSWER/FAIL lines, ALWAYS emit the complete diagnostic format `arr=[...]:exp=[...]:got=[...]` — never emit a bare `n=` or just a count. `exp=` (expected) must be shown as an array (`exp=[...]`), and `got=` must be computed by re-traversing the result list and converting it to an array BEFORE printing (the traversal pointer is null after the first validation pass, so capture the result into an array first). Also surface extra input params in the FAIL line as `data=`, `target=`, `op=`, `del=` (and any other relevant params) so the student can fully reproduce the failing case. Confidence: 0.90
